doctype html
html(lang='en')
  include ./head.pug
  body
    header
      h1 
        a(href='/') Job Finder
      #logContainer
        h4
          a(href='/profile') profile
        h4
          a(href='/favorites') favorites 
        h4
          a(href='/') Ofertas aplicadas
          //- Solo visibles par aadministrador
        h4
          a(href='/api/users') users(admin)
        h4
          a(href='/dashboard') dashboard(admin)
          //----------------------------------
        form(action="/api/logout" method="post")
          input(type="submit")
        h4
          a(href='/changepassword') Cambiar contraseña
        h4 
          a(href='/recoverpassword') Recuperar contraseña
    main
      div#mainContainer
        h1#h1Slogan NOMBRE DE USUARIO
        h3#h3Slogan
          | Tenemo nuevas ofertas publicadas, ¿les echas un vistazo?
        div#searchFormContainer
        div#searchContainer
          input(id="inputValue" placeholder="Introduce tu busqueda")
          button#searchButton Click
    script(type='text/javascript', src='../scripts/homeNavigation.js')
include ./footer.pug