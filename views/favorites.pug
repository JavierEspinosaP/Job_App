doctype html
html(lang='en')
  include ./head.pug
  body
    header
      h1 
        a(href='/') Job Finder
      #logContainer
        h4
          a(href='/profile') profile
        h4
          a(href='/dashboard') dashboard(admin)
        h4
          a(href='/') Ofertas aplicadas
          //- Solo visibles par aadministrador
        h4
          a(href='/api/users') users(admin)
        h4
          a(href='api/dashboard') dashboard(admin)
          //----------------------------------
        h4
          a(href='/api/logout') Logout
    main
      div#mainContainer
        h1#h1Slogan FAVORITES
        h3#h3Slogan Estos son tus favoritos
        each fav in userFavs
          p=fav.reference_offer

    //- //script(type='text/javascript', src='../scripts/homeNavigation.js')
include ./footer.pug