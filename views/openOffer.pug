doctype html
html(lang='en')
  include ./head.pug
  body
    header
      div.header
        h1 
          a(href='/') Job Finder
        #logContainer
        div.logContainer
          h4
            a(href='/profile') Perfil
          h4
            a(href='/favorites') Favoritos 
          h4
            a(href='/') Ofertas aplicadas
          h4
            a(href='/api/users') Usuarios
          h4
            a(href='api/dashboard') Tablón
          form(action="/api/logout" method="post")
            button.clickBtnlogOut.logoutBtn(type="submit") Salir
    main
      div#h2SignUpContainer
        h2#h2SignUp #{offer[0].title}
        hr
        div#signUpFormContainer
          p.pOffer #{offer[0].date}
          h4.h3Offer #{offer[0].budget}
          p.pOffer #{offer[0].description}
    script(type='text/javascript', src='../scripts/homeNavigation.js')
include ./footer.pug